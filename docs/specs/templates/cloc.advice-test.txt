First, look for tests that can be parameterized or share fixtures, and extract repetitive setup into helper functions.

Then split by the semantic area they cover (target 200–330 lines each). Group tests by the concern they exercise and place each group in a subfolder or its own sibling test file.
